# AquaTrade Documentation Complete v0.4

This package replaces v0.3 after a consistency audit.

## Copy into the frontend repository

Copy the entire `docs` folder into:

```text
aquatrade-fe/docs/
```

Then merge `CLAUDE-PROJECT-INSTRUCTIONS.md` into the repository root `CLAUDE.md`.

## Start Here

```text
docs/README.md
```

## Important

- White Belt Test XLM is public and does not require application login.
- Login is currently a future business-MVP design prototype.
- `AquaTrade` is the platform; `AquaTrade Pay` is the White Belt-facing payment experience.
- The route pattern is `/{locale}/test-xlm`.
